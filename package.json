{
  "name": "avalon-workspace",
  "packageManager": "yarn@3.2.1",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.1.10"
  }
}
